# 组态工具（maotu-webtopo）专业评估报告

> 评估对象：一次接线图组态编辑器（Vue 3 + TypeScript + Element Plus + Pinia 风格全局单例 Store + Electron）
> 评估范围：`src/components/mt-edit/`、`src/components/mt-dzr/`、`src/components/mt-preview/`、`src/composables/`、`src/views/edit/`
> 评估方法：源码通读 + 模块级调研，结论均附文件与关键行号

## 一、执行摘要

该组态工具已经具备一套相当完整的"底层能力骨架"：组件拖拽创建、缩放/旋转手柄、自由/直角连线、组合/解组、多选对齐、参考线与吸附、撤销重做、设备测点绑定、导出与实时预览均已实现。但在"用户可见的交互入口"与"面向组态场景的核心能力闭环"上存在明显落差。

四个维度的主要问题可归纳为：第一，用户体验上缩放/缩略图/百分比、网格吸附开关、图层管理这些编辑器标配能力在界面上几乎是缺失的；第二，交互逻辑上框选不支持加选、旋转无吸附、等比缩放的 Shift 行为与主流工具相反、连线绘制不吸附网格；第三，功能完整性上"事件"与"动画"面板被注释隐藏（数据结构已存在但 UI 不可用），属性配置控件类型仅有 7 种，数据绑定强依赖宿主通过插槽注入；第四，架构合理性上撤销重做采用"全场景深拷贝"、全局响应式单例直接持有整个画布 JSON、命令式 DOM 读取混入 Vue 渲染管线，这些在大场景（数百图元的一次接线图）下会直接引发卡顿与维护困难。

下面按四维度展开痛点，再给出核心场景的优化与创新建议、功能优先级排序，最后按模块给出可落地清单。

## 二、四维度现状与痛点

### 维度一：用户体验（UX）

缩放与导航能力在界面上几乎不可见。底部状态栏（`mt-edit/index.vue` 内联 footer）只展示最近更新时间、场站名、图纸名与连接状态灯；真正的 `footer-panel/index.vue` 组件仅渲染一行版权且从未被渲染（死代码）。缩放只能靠 Ctrl+滚轮（`main-panel/index.vue:1438`），既无百分比显示、无放大/缩小/适应屏幕按钮，也无缩略图小地图。

网格/吸附/参考线开关没有 UI 入口。`globalStore.canvasCfg` 中的 `guide`、`adsorp`、`gridCfg` 默认是关闭的（`store/global.ts`），但编辑器界面上找不到任何开启它们的按钮，用户无法从 UI 感知"对齐网格"能力的存在。

图层（z-order）无可视化面板。结构树只在点击"组件树"后从抽屉弹出（`mt-edit/index.vue:136`），编辑过程中无法常驻看到层级关系，而 z-order 调整仅存在于右键菜单（`main-panel/index.vue:1030-1036`），工具栏无对应入口，两条操作路径能力不一致。

工具栏信息密度失衡且缺少可发现性。对齐功能藏在 hover 弹层里（`header-panel/index.vue:94-169`），且仅暴露左/右/水平居中/垂直居中 4 种，但底层 `main-panel.onAlignSelected` 实际支持 `top/bottom/horizontal-distribution/vertical-distribution` 共 8 种——功能已实现却不可见。所有工具栏按钮均无快捷键标注，而实际快捷键（Ctrl+Z/Y、Delete、Ctrl+G/U、方向键）散落在 `main-panel.onKeydown`，用户无法从界面学习。

右键菜单存在可用性缺陷：定位坐标直接取自 `menuInfo.left/top`（`context-menu/index.vue:41-42`），靠近屏幕边缘会溢出视口被裁剪，且无分组分隔、点击外部关闭逻辑依赖后续 mousedown 重置，存在"菜单卡住"风险。

视觉一致性细节：左侧图元库缩略图在亮色模式下强制 `bg-amber-50`（`left-aside/index.vue:43`），在浅色画布上反而不易辨识。

### 维度二：交互逻辑

框选是覆盖式而非累加式。`main-panel.onSelectedAreaMouseUp`（`:550-581`）每次框选都会重算全量 `done_json` 的 active，不支持 Shift 加选/减选，也无法与图元级 Ctrl 多选形成统一的多选状态机。此外框选命中用旋转后的外接矩形（AABB）再反算（`main-panel:553-567`），导致旋转元素的命中区域大于视觉区域，易误选。

旋转无角度吸附，等比缩放 Shift 行为反直觉。旋转手柄 `mt-dzr/rotate-handle.vue` 的 `alignToGrid(initAngle, ...)` 默认 grid=1，即逐度连续旋转，无 Shift 15°/45° 吸附，也无角度输入框。等比缩放默认开启（`mt-dzr/index.vue:91`），但按住 Shift 反而临时取消等比（`resize-handle.vue:145-146`），与 Figma/PowerPoint 等"Shift=等比"的通用惯例相反。

连线绘制不吸附网格。`line-render/index.vue:308-309` 中坐标换算硬编码 `alignToGrid(..., 1)` 并注释"体验不好所以固定为 1"，导致连线与图元的网格/吸附设置不一致，画出的线常与图元不在同一网格基准上，视觉错位。

强前置校验阻断创作流。`main-panel.ensureStationForDrawing`（`:286-296`）要求"未添加场站禁止在画布绘制任意图形"，但这对只想先画一张示意拓扑图的用户是一种强制跳转，且拖拽过程中才拦截并复位意图，体验突兀。

多选移动与连线跟随的实时开销高。多选移动时 `render-core.onItemMove`（`:204-218`）对每一个选中项每帧调用 `document.getElementById(m).getBoundingClientRect()`；连线跟随、吸附计算也都依赖实时 DOM 测量，未做节流或 `requestAnimationFrame` 批处理。

连线与图元的绑定关系脆弱。复制/粘贴/移动/解组时，`bind_anchors` 被分散在 `main-panel` 的多个方法里手工置空（`:719-738`、`:1068-1075`、`:1405-1409` 等），且部分跟随逻辑被注释禁用（`line-render` 中 adsorb 段被整段注释），数据一致性靠散落的 if 修补，是主要 bug 来源。

### 维度三：功能完整性

事件与动画能力"半成品化"。右侧属性面板的"动画配置"与"事件"两个 tab 被注释掉（`select-item-setting.vue:72-84`），但底层数据结构 `common_animations`、`events` 均已存在并参与渲染/导出。这意味着最体现"组态"价值的交互联动（点击跳转、数据变化触发动画、告警闪烁）目前无法通过 UI 配置，能力被白白闲置。

属性配置控件类型过窄。`select-item-props-setting.vue` 仅支持 `input/number/select/switch/color/textArea/jsonEdit` 七种。缺少滑块、字体选择器、图片上传、渐变/描边、图标选择、日期时间等高级控件，难以满足一次接线图里大量"数值+单位+状态色"的展示需求。

数据绑定强耦合宿主。右侧"绑定"tab 的内容完全依赖宿主通过 `#deviceBind` 插槽注入（`select-item-setting.vue:86`、`mt-edit/index.vue` 透传自 `views/edit`），编辑器自身不提供内置的设备/测点绑定面板；`device-template` 仅负责"导入 Excel 配置测点模板"，绑定动作本身外置。这导致编辑器作为独立产品时数据绑定不可用。

无测点面板自定义与 KPI 模板。拖入设备图元时自动生成"设备名称标签"与 kv 面板（`main-panel.createDeviceLabel`、`:423-425`），但这些自动生成物一旦生成即与常规图元无异，用户无法重新进入"绑定编辑"，且 `select-item-setting.vue:319` 通过 `isDevicePanelGenerated` 直接隐藏了绑定 tab，形成"生成后不可改"的死胡同。

缺失的通用编辑器能力：无撤销粒度控制（一次拖拽应合并为一步，但 `render-core.onLineMouseUp` 还用 `setTimeout 1000` 补历史，存在重复/遗漏）、无快捷键自定义、无模板/组件市场、无协作与版本管理、无移动端适配、无障碍（a11y）基本空白。

### 维度四：架构合理性

全局响应式单例直接持有整个画布。`globalStore`（`store/global.ts`）以模块级 `reactive` 持有 `done_json`（全量场景）、`selected_items_id`、`canvasCfg`、`real_time_data` 等，`dzrStore`、`cacheStore`、`contextMenuStore` 也是同级单例。任何深层字段变更都会触发全量依赖追踪与调度，大场景响应式开销高，且单例难以测试与复用。

撤销重做采用"全场景深拷贝"。`cache.ts` 每次操作都对整个 `done_json` 执行 `objectDeepClone` 压入 `history` 数组，上限仅 20 步。一次接线图常有数百图元，频繁移动/缩放会反复深拷贝全量数据，内存与 GC 压力巨大，是大场景卡顿的核心元凶之一。

命令式 DOM 读取混入 Vue 管线。除上文提到的 `getBoundingClientRect` 调用外，`mt-dzr/index.vue:161` 在 mousedown 时同步读取 `parentElement.clientWidth`，`selected-area` 在每次 mousemove 都读 `targetDom.getBoundingClientRect()`（`selected-area/index.vue:29`），均违反响应式/渲染分离原则并带来布局抖动。

鼠标监听逻辑 5+ 处重复实现。`mt-dzr/utils/autoDestroyMouseMove`、`drag-canvas/index.vue`、`selected-area`、`main-panel`、`line-render`、`draw-line-render` 各自内联了一套 add/removeEventListener，应统一为一个 composable。

渲染组件大面积重复。`line-render` 与 `draw-line-render` 的 marker/箭头 `<defs>`、`<path>`、坐标换算、getBoundingClientRect 逻辑几乎逐行重复（`draw-line-render.vue:17-96` vs `line-render.vue:16-96`），应抽取为共享的连线渲染核心。

组件库配置偏"代码驱动"。`config.ts` 通过 `import.meta.glob` 静态导入 SVG 集合，再用硬编码的 `Set` 做分类映射（sysComponent/newEnergy/storage/...），新增一类组件需要改代码而非配置化/数据库化；自定义图元虽支持本地库上传（`use-custom-symbols`），但与内置库不在同一注册体系。

预览与编辑耦合但数据注入方式脆弱。预览 `mt-preview/index.vue` 复用 `render-core`，但实时数据通过 `window.$setItemAttrByID` 等全局函数注入（`:192-197`），预览组件与外部环境强耦合，难以在纯前端/嵌入场景下独立复用。

## 三、核心场景优化方向与创新建议

### 1. 组件拖拽与创建

优化方向：拖入时在光标处显示落点幽灵预览与最近的网格/吸附参考线；支持从图元库双击直接放置于画布中心；拖入后默认尺寸应"智能贴合"（设备类带标签、母线按画布 70% 宽，已有雏形，可扩展为按分类预设）。批量场景支持一次拖入多个图元并保持相对间距。

创新建议：图元库增加"最近使用 / 收藏 / 搜索热词 / 按场站类型推荐"；支持把一组图元存为"模板组合"并像单个图元一样拖入（当前 `instantiateTemplateContent` 已有实例化能力，只需在左侧库里暴露"我的模板"分类）；引入"智能布局"——框选一组设备后一键按电气主接线典型结构排布。

### 2. 属性配置

优化方向：补齐控件类型——滑块（数值区间）、字体选择器（字体/字重/行高）、图片上传、渐变与描边、图标选择、日期时间。恢复并强化"事件"与"动画"两个被注释的 tab，这是组态工具区别于普通绘图软件的核心卖点。支持多选中统一编辑（当前仅单选中才有属性面板，`right-aside/index.vue:3-11`）。

创新建议：引入"可视化表达式/公式绑定"，例如某文本图元的显示值 = `测点A - 测点B` 或 `IF(告警, 'red', 'green')`；引入"条件样式"，按实时数据阈值自动切换颜色/显隐/动画；属性面板支持"按组件类型分组折叠"与"常用属性置顶"。

### 3. 数据绑定

优化方向：把设备/测点绑定做成编辑器内置面板，不再依赖宿主插槽。绑定关系在画布上可视化——选中连线或设备时高亮其绑定的图元与测点。支持"测点面板"二次编辑（打破当前生成后不可改的死胡同）。

创新建议：支持多种数据源（MQTT / WebSocket / HTTP 轮询 / OPC-UA）与"模拟数据源"（随机值、脚本、历史回放），让用户在无后端时也能演示完整效果；引入"历史曲线"与"告警阈值"组件，绑定后自动出图；支持双向绑定（下发控制指令），向 SCADA/HMI 闭环靠拢。

### 4. 实时预览

优化方向：将当前"弹窗预览"升级为编辑器内的"实时模式"开关（同源渲染、零成本切换），避免每次都重新加载 `exportJson`（`preview-dialog.vue` 用 `onOpened` 二次加载规避尺寸为 0 的问题，体验有顿挫）。预览应支持响应式自适应（已有 `calcFitScale`，可加"按设备像素/分辨率预设"）。

创新建议：内置"数据模拟器"面板，可在预览中注入脚本化实时数据；预览通道与"发布/权限"打通，支持生成只读分享链接或嵌入 iframe；引入"版本快照对比"——保存历史节点并并排预览差异。

## 四、功能优先级与缺失关键功能

P0（止血/核心闭环，建议本迭代）：
- 缩放 UI（百分比/放大缩小/适应屏幕）、网格·吸附·参考线全局开关
- 撤销重做稳定性（增量历史或结构化克隆，避免全量深拷贝卡顿）
- 恢复"事件"与"动画"面板并打通到导出/预览
- 数据绑定内置化（不依赖宿主插槽），并支持绑定后二次编辑

P1（体验补齐）：
- 常驻图层面板 + 工具栏 z-order 按钮
- 属性面板多选中统一编辑 + 控件类型扩展
- 对齐全 8 种（顶/底/左右分布）在工具栏可见
- 连线绘制吸附网格、旋转角度吸附、等比缩放 Shift 行为修正
- 框选 Shift 加选/减选

P2（差异化与平台化）：
- 图元库增强（收藏/最近/模板组合分类）
- 多数据源 + 模拟数据 + 历史曲线 + 告警
- 协作、版本管理、模板市场
- 性能优化（渲染虚拟化/增量历史/批处理 DOM 读取）
- 移动端适配与无障碍（a11y）

缺失的关键功能清单：图层管理与拖拽调序、缩略图导航小地图、对齐与分布全量、多数据源接入、权限与发布通道、协作与版本、移动端与触屏适配、键盘可达性（a11y）、快捷键自定义。

## 五、按模块的可落地建议

模块一：顶部工具栏 `header-panel`
- 增加缩放控件组（− / 百分比输入 / + / 适应）、网格·吸附·参考线开关、z-order 按钮（置顶/上移/下移/置底）、对齐全 8 种按钮。
- 为所有按钮增加快捷键 tooltip 标注；统一键盘快捷键与菜单项，消除"工具栏与右键菜单能力不一致"。
- 把 `alignSelected` 等内联业务逻辑下沉到 store/事件，避免 UI 组件持有状态。

模块二：底部状态栏 `footer-panel` / 内联 footer
- 真正实现缩放百分比、适应屏幕、缩略图小地图（可点击定位）；删除 `footer-panel` 死代码或赋予其真实职责（状态栏 + 缩放 + 缩略图）。
- 缩放数值与 `globalStore.canvasCfg.scale` 双向联动。

模块三：左侧图元库 `left-aside`
- 增加收藏/最近使用/搜索热词；自定义分类支持拖拽排序；新增"我的模板组合"分类，复用 `instantiateTemplateContent`。
- 亮色模式下缩略图背景改为中性色，提升辨识度。

模块四：右侧属性面板 `right-aside` / `select-item-setting` / `select-item-props-setting`
- 恢复事件与动画 tab，补齐 7 种以外的控件，支持多选中统一编辑，内置设备/测点绑定面板（解耦宿主插槽）。
- 打破"设备面板生成后隐藏绑定 tab"的死胡同（`isDevicePanelGenerated` 逻辑改为"可重新进入绑定编辑"）。

模块五：画布交互 `main-panel` / `render-core` / `mt-dzr`
- 框选支持 Shift 加选/减选，命中判定改用精确多边形而非 AABB。
- 旋转加 Shift 角度吸附并补角度输入框；等比缩放默认开启但 Shift 保持等比（与惯例一致）；连线绘制恢复网格吸附。
- 性能：DOM 测量用 `requestAnimationFrame` 批处理；撤销重做改为基于增量补丁（如 immer/patch 或结构克隆单步）而非全量深拷贝；统一鼠标监听为单一 composable；抽取 `line-render`/`draw-line-render` 共享核心。

模块六：数据绑定 `device-template` / `useDeviceBinding`
- 绑定面板内置化；绑定关系画布可视化；支持多数据源与模拟数据；开放"测点面板"二次编辑入口。

模块七：实时预览 `mt-preview`
- 增加编辑器内"实时模式"开关，避免弹窗二次加载；内置数据模拟器；预览与发布/权限打通；响应式按分辨率预设。

## 六、结论

maotu-webtopo 已经是一座"地基扎实但毛坯"的组态编辑器：拖拽、连线、组合、对齐、历史、数据绑定、预览这些最难的基础能力都已就位，工程完成度高于一般同类项目。真正的差距不在"能不能做"，而在"好不好用、能不能闭环、能不能撑住大场景"。

最值得优先投入的三件事是：把缺失的界面入口（缩放、网格吸附、图层、对齐全量）补上；把已建好却被注释/外置的能力（事件、动画、数据绑定）真正交付给用户；以及从架构上消除"全量深拷贝历史 + 全局单例 + 命令式 DOM 读取"这三类会随场景规模线性恶化的性能与维护隐患。完成这三项，产品即可从"内部可用"迈向"对外可卖"。

## 七、参考源码位置

- 全局状态与历史：`src/components/mt-edit/store/global.ts`、`src/components/mt-edit/store/cache.ts`、`src/components/mt-edit/store/config.ts`
- 画布交互与快捷键：`src/components/mt-edit/components/layout/main-panel/index.vue`
- 渲染核心：`src/components/mt-edit/components/render-core/index.vue`
- 缩放/旋转手柄：`src/components/mt-dzr/`（index.vue、resize-handle.vue、rotate-handle.vue、utils/autoDestroyMouseMove.ts）
- 属性面板：`src/components/mt-edit/components/layout/right-aside/`（select-item-setting.vue、select-item-props-setting.vue）
- 图元库：`src/components/mt-edit/components/layout/left-aside/index.vue`
- 数据绑定：`src/composables/useDeviceBinding.ts`、`src/components/mt-edit/components/layout/device-template/index.vue`、`src/views/edit/index.vue`（deviceBind 插槽）
- 预览：`src/components/mt-preview/index.vue`、`src/components/mt-preview/preview-dialog.vue`
- 连线：`src/components/mt-edit/components/line-render/index.vue`、`src/components/mt-edit/components/draw-line-render/index.vue`
